{
  "name": "free-music-finder",
  "private": true,
  "version": "1.0.0",
  "description": "wagenhoffer.dev Free Music Finder for Creative Commons Bandcamp music (no build tooling)",
  "scripts": {
    "generate:license-counts": "node tools/data/generate-license-counts.js",
    "generate:license-counts:check": "node tools/data/generate-license-counts.js --check",
    "version-assets": "node scripts/version-assets.js",
    "start": "npm run generate:license-counts && npm run version-assets && node server.js",
    "dev": "npm run generate:license-counts && npm run version-assets && node server.js",
    "sync:urls": "node tools/data/sync-upstream-urls.js && npm run generate:license-counts",
    "sync:urls:check": "node tools/data/sync-upstream-urls.js --check",
    "check:urls": "node tools/data/check-urls-integrity.js",
    "deploy:live": "bash scripts/deploy-live.sh",
    "deploy:live:dry": "bash scripts/deploy-live.sh --dry-run"
  },
  "engines": {
    "node": ">=18"
  }
}
